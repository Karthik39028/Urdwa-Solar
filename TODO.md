# TODO

- [ ] Create `src/components/InitialLoader.jsx` (logo + loading line overlay)
- [ ] Render `<InitialLoader />` inside `src/App.jsx`
- [ ] Add/adjust CSS in `src/styles.css` if required
- [ ] Run `npm run dev` and verify loader shows then disappears on full window load

